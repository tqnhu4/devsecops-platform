apiVersion: v1
kind: ConfigMap

metadata:
  name: argocd-dashboard
  namespace: monitoring

  labels:
    grafana_dashboard: "1"

data:
  argocd.json: |
    {
      "annotations": {
        "list": []
      },
      "panels": [
        {
          "type": "stat",
          "title": "Applications"
        }
      ],
      "title": "ArgoCD Dashboard",
      "version": 1
    }