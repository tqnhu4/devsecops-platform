apiVersion: v1

kind: ConfigMap

metadata:

  name: fluent-bit-config

  namespace: monitoring

data:

  fluent-bit.conf: |

    [SERVICE]

        Flush 1

    [INPUT]

        Name tail

        Path /var/log/containers/*.log

    [OUTPUT]

        Name stdout